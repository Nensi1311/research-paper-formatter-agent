# ScholarEnv v6 — server dependencies
# Pinned to versions known to coexist with openenv-core 0.2.x

# OpenEnv compliance — without this, server/app.py silently falls through
# to the standalone-FastAPI branch and the /reset, /step, /ws endpoints
# served by openenv.core.env_server.http_server.create_app() are missing.
openenv-core>=0.2.3

# Web stack (compatible with openenv-core's pins)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0
pyyaml>=6.0.1

# OpenAI client used by inference.py against router.huggingface.co/v1
openai>=1.35.0

# HackNU gem: Levenshtein matching for graders (much better than difflib)
thefuzz>=0.22.0
python-Levenshtein>=0.25.0   # makes thefuzz ~100x faster, optional but cheap

# Citation cache uses sqlite3 from stdlib — no install needed.

# Training stack lives in the notebook, not on the HF Space:
# pip install unsloth trl transformers datasets torch accelerate peft
